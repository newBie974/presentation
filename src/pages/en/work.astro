---
import MainLayout from "@/layouts/MainLayout.astro";
import Nav from "@/components/organisms/Nav.astro";
import Footer from "@/components/organisms/Footer.astro";
import SectionLabel from "@/components/atoms/SectionLabel.astro";
import Highlight from "@/components/atoms/Highlight.astro";
import OfferTiers from "@/components/organisms/OfferTiers.astro";
import OfferSupport from "@/components/organisms/OfferSupport.astro";
import TrustBand from "@/components/organisms/TrustBand.astro";
import ProcessStep from "@/components/molecules/ProcessStep.astro";
import FAQItem from "@/components/molecules/FAQItem.astro";
import StatusBadge from "@/components/atoms/StatusBadge.astro";
import { faq } from "@/data/faq";
import { testimonials } from "@/data/testimonials";
import Testimonial from "@/components/molecules/Testimonial.astro";
import CalEmbed from "@/components/molecules/CalEmbed.astro";
import JsonLd from "@/components/atoms/JsonLd.astro";
import {
  buildPersonJsonLd,
  buildFAQJsonLd,
  buildProfessionalServiceJsonLd,
} from "@/lib/jsonLd";

const locale = "en" as const;
const title =
  "Work together — Aymeric Dijoux, freelance AI builder · Réunion & Paris";
const description =
  "Freelance developer based in Réunion Island (France) and Paris, AI app expert. Aymeric Dijoux takes 1-2 missions per half-year — mobile, web, AI prototype. Remote, fixed price or day rate, free 30-min intro call.";
const calcomUrl =
  import.meta.env.PUBLIC_CALCOM_URL ?? "https://cal.eu/aymeric-dijoux/intro";
const calLink = "aymeric-dijoux/intro";

const processSteps = [
  {
    roman: "i" as const,
    title: "Intro call (30 min, free)",
    desc: "You explain the project, I tell you if I'm the right fit and how long it takes.",
  },
  {
    roman: "ii" as const,
    title: "Written brief (48h)",
    desc: "I send a short doc: scope, timeline, milestones, fixed price or day rate.",
  },
  {
    roman: "iii" as const,
    title: "Kick-off & build",
    desc: "Async daily or weekly (Slack/Linear). Demo each week. Code on your GitHub from day 1.",
  },
  {
    roman: "iv" as const,
    title: "Ship & handover",
    desc: "Production rollout + clean handover (docs, Loom, clean repo). 2 weeks of support included.",
  },
];
---

<MainLayout
  title={title}
  description={description}
  locale={locale}
  routeKey="work"
>
  <Fragment slot="head">
    <JsonLd
      data={[
        buildPersonJsonLd(),
        buildProfessionalServiceJsonLd(locale),
        buildFAQJsonLd(faq.map((f) => ({ q: f.q[locale], a: f.a[locale] }))),
      ]}
    />
  </Fragment>
  <Nav slot="nav" locale={locale} current="work" />

  <section class="page-hero">
    <SectionLabel>WORK TOGETHER</SectionLabel>
    <h1>
      Freelance <Highlight>AI</Highlight> developer — your product, Figma to store.
    </h1>
    <p class="deck">
      I design, build and ship your mobile or web app powered by AI, no
      subcontracting. You talk straight to the person who writes the code.
    </p>
    <p class="values">
      Building with you, not just for you. Based in <Highlight
        >Réunion Island</Highlight
      > & Paris, remote everywhere — 1-2 missions per half-year.
    </p>
    <StatusBadge label="Available H2 2026 · 1 slot left" />
  </section>

  <OfferTiers locale={locale} />
  <OfferSupport locale={locale} />
  <TrustBand locale={locale} />

  <section class="section">
    <SectionLabel number="02">HOW IT WORKS</SectionLabel>
    <h2>The <Highlight>process</Highlight></h2>
    {
      processSteps.map((s) => (
        <ProcessStep roman={s.roman} title={s.title} desc={s.desc} />
      ))
    }
  </section>

  <section class="section">
    <SectionLabel number="03">THEY WORKED WITH ME</SectionLabel>
    <h2>A few <Highlight>testimonials</Highlight></h2>
    <div class="testimonials">
      {testimonials.map((t) => <Testimonial testimonial={t} locale={locale} />)}
    </div>
  </section>

  <section class="section">
    <SectionLabel number="04">FAQ</SectionLabel>
    <h2>The questions <Highlight>I hear most</Highlight></h2>
    {
      faq.map((item, i) => (
        <FAQItem q={item.q[locale]} a={item.a[locale]} open={i === 0} />
      ))
    }
  </section>

  <section class="section booking">
    <SectionLabel number="05">BOOK A CALL</SectionLabel>
    <h2>Pick a <Highlight>slot</Highlight></h2>
    <p class="lead">
      30 minutes, free, no commitment. If I'm not the right fit, I'll tell you
      and recommend someone.
    </p>
    <CalEmbed calLink={calLink} />
  </section>

  <section class="end-cta">
    <h2>None of the slots work?</h2>
    <p>
      Email me directly:
      <a href="mailto:aymeric@dijoux.dev">aymeric@dijoux.dev</a>
    </p>
    <p class="alt">
      Or open the calendar full-screen:
      <a href={calcomUrl} target="_blank" rel="noopener noreferrer"
        >cal.eu/aymeric-dijoux/intro</a
      >
    </p>
  </section>

  <Footer slot="footer" locale={locale} />
</MainLayout>

<style>
  .page-hero {
    padding: 64px 48px 32px;
    border-bottom: var(--border-w) solid var(--color-border);
    max-width: var(--container-content);
    margin: 0 auto;
  }
  h1 {
    font-size: clamp(32px, 6vw, 48px);
    line-height: 1;
    font-weight: 700;
    letter-spacing: -0.04em;
    margin: 14px 0;
  }
  .deck {
    font-size: 17px;
    line-height: 1.6;
    color: var(--color-text-muted);
    max-width: 560px;
    margin: 0 0 12px;
  }
  .values {
    font-size: 14px;
    line-height: 1.55;
    color: var(--color-text-muted);
    max-width: 560px;
    margin: 0 0 20px;
  }
  .section {
    padding: 40px 48px;
    border-bottom: var(--border-w) solid var(--color-border);
    max-width: var(--container-content);
    margin: 0 auto;
  }
  .section h2 {
    font-size: 24px;
    font-weight: 700;
    letter-spacing: -0.02em;
    margin: 12px 0 18px;
    line-height: 1;
  }
  .testimonials {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 12px;
  }
  @media (max-width: 720px) {
    .testimonials {
      grid-template-columns: 1fr;
    }
  }
  .booking .lead {
    font-size: 15px;
    line-height: 1.65;
    color: var(--color-text-muted);
    margin: 0 0 20px;
    max-width: 580px;
  }
  .end-cta {
    background: var(--color-inverse-bg);
    color: var(--color-inverse-fg);
    padding: 56px 48px;
    text-align: center;
  }
  :global([data-theme="dark"]) .end-cta {
    box-shadow: inset 0 2px 0 var(--color-accent);
  }
  .end-cta h2 {
    font-size: clamp(24px, 5vw, 32px);
    font-weight: 700;
    letter-spacing: -0.03em;
    margin: 0 0 14px;
    line-height: 1;
    color: var(--color-inverse-fg);
  }
  .end-cta p {
    font-size: 14px;
    color: var(--color-inverse-muted);
    max-width: 460px;
    margin: 0 auto 20px;
    line-height: 1.6;
  }
  .end-cta a {
    color: var(--color-accent);
    text-decoration: underline;
    text-decoration-thickness: 2px;
    text-underline-offset: 4px;
  }
  .end-cta .alt {
    font-family: var(--font-mono);
    font-size: 12px;
    color: var(--color-inverse-muted);
    margin-top: 12px;
  }
  @media (max-width: 720px) {
    .page-hero,
    .section,
    .end-cta {
      padding-left: 20px;
      padding-right: 20px;
    }
  }
</style>
